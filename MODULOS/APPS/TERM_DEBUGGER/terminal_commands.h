#include "BRTOS.h"
#include "terminal.h"

void cmd_top(char *param);
extern const command_t top_cmd;

void cmd_rst(char *param);
extern const command_t rst_cmd;

void cmd_ver(char *param);
extern const command_t ver_cmd;

void cmd_snf(char *param);
extern const command_t snf_cmd;
